export interface ServiceCarousalType {
    ServiceCarousalData: {
        heading: string,
        desc: string,
        slag: string,
        buttonData: string,
        buttonBg: string,
        slideImages: {
            id: number,
            imageUrl: string
        }[]
    }
}